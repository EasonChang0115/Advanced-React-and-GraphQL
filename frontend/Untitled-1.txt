# JavaScript ES6 基礎功\n\n
> [name=仲豪] 

## 取代var 的let 與 const

和var不同的地方在於作用域的不同，var宣告出來的變數是全域變數，而let和const會限制在他原本的Scope中。

* var

```javascript=
for(var i = 0; i < 3; i++){}
console.log(i);
```
這時候i會顯示3